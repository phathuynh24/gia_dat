# Data & DB — chỉ commit listings.db (đủ chạy/deploy). File crawl thô không cần.
data/crawl_*.json
data/*_log.txt

# Secrets — KHÔNG BAO GIỜ commit
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Playwright
/test-results/
