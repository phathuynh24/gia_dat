# Web runtime (đủ để chạy/deploy dashboard)
Flask==3.1.0
gunicorn==23.0.0

# Crawler & parse-by-Claude là việc THỦ CÔNG, chạy ở máy local — KHÔNG cần trên server.
# Cài riêng khi crawl:  pip install -r requirements-crawl.txt
