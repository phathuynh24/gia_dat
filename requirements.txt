# Web app
Flask==3.1.0

# Crawler (chạy thủ công, optional cho việc lấy data thật)
playwright==1.49.0

# Optional: parse bằng Claude API (nếu có ANTHROPIC_API_KEY). Parser mặc định dùng regex, không cần cài.
anthropic==0.42.0
